/**
 * @file config.h
 * @brief ek-kor Configuration Header
 *
 * Auto-generated by CMake - DO NOT EDIT
 */

#ifndef EKK_CONFIG_H
#define EKK_CONFIG_H

/* ==========================================================================
 * Version
 * ========================================================================== */

#define EKK_VERSION_MAJOR       @ek-kor_VERSION_MAJOR@
#define EKK_VERSION_MINOR       @ek-kor_VERSION_MINOR@
#define EKK_VERSION_PATCH       @ek-kor_VERSION_PATCH@
#define EKK_VERSION_STRING      "@ek-kor_VERSION@"

/* ==========================================================================
 * Core Configuration
 * ========================================================================== */

/** Number of CPU cores (1 = single-core, 6 = TC397XP) */
#define EKK_MAX_CORES               @EKK_MAX_CORES@

/** Maximum tasks per core */
#define EKK_MAX_TASKS_PER_CORE      @EKK_MAX_TASKS_PER_CORE@

/** Total maximum tasks */
#define EKK_MAX_TASKS               (EKK_MAX_CORES * EKK_MAX_TASKS_PER_CORE)

/** Default task stack size (bytes) */
#define EKK_DEFAULT_STACK_SIZE      @EKK_DEFAULT_STACK_SIZE@

/** Minimum task stack size (bytes) */
#define EKK_MIN_STACK_SIZE          @EKK_MIN_STACK_SIZE@

/** System tick frequency (Hz) */
#define EKK_TICK_FREQ_HZ            @EKK_TICK_FREQ_HZ@

/** Tick period (microseconds) */
#define EKK_TICK_PERIOD_US          (1000000 / EKK_TICK_FREQ_HZ)

/* ==========================================================================
 * IPC Configuration
 * ========================================================================== */

/** Message queues per core */
#define EKK_MAX_QUEUES              @EKK_MAX_QUEUES@

/** Cross-core mailboxes */
#define EKK_MAX_MAILBOXES           @EKK_MAX_MAILBOXES@

/** Maximum message size (bytes) */
#define EKK_MAX_MSG_SIZE            @EKK_MAX_MSG_SIZE@

/* ==========================================================================
 * Synchronization Configuration
 * ========================================================================== */

/** Mutexes per core */
#define EKK_MAX_MUTEXES             @EKK_MAX_MUTEXES@

/** Semaphores per core */
#define EKK_MAX_SEMAPHORES          @EKK_MAX_SEMAPHORES@

/* ==========================================================================
 * Feature Flags
 * ========================================================================== */

/** EDF (Earliest Deadline First) scheduling */
#cmakedefine01 EKK_USE_EDF

/** Track per-task execution time */
#cmakedefine01 EKK_TRACK_TIME

/** Deadline miss detection */
#cmakedefine01 EKK_CHECK_DEADLINE

/** Debug mode */
#cmakedefine01 EKK_DEBUG

/* ==========================================================================
 * Derived Configuration
 * ========================================================================== */

/** Multi-core support enabled */
#if EKK_MAX_CORES > 1
    #define EKK_MULTI_CORE          1
#else
    #define EKK_SINGLE_CORE         1
#endif

/** Maximum priority level */
#define EKK_MAX_PRIORITY            255

/** Idle task priority */
#define EKK_PRIORITY_IDLE           0

/** Kernel priority */
#define EKK_PRIORITY_KERNEL         255

/** Task name maximum length */
#define EKK_TASK_NAME_LEN           16

#endif /* EKK_CONFIG_H */
